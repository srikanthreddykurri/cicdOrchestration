pipeline {
    agent {label "test"}

    stages{
        stage("compile"){
            steps{
            echo "compiling the code"
            }
    }
        stage("test"){
            steps{
                echo "testing the code"
            }
        }

         stage("deploy"){
            steps{
                echo "deploying the code"
            }
        }
    }
}